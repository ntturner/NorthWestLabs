@model IEnumerable<WebApplication1.Models.Test>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TestDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Protocol)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WageCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalMaterialsCost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OverallCost)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TestDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Protocol)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WageCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalMaterialsCost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OverallCost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TestID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TestID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TestID })
        </td>
    </tr>
}

</table>
