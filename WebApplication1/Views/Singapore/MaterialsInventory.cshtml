@model IEnumerable<WebApplication1.Models.Materials>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MaterialDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuantityOnHand)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaterialDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantityOnHand)
            </td>
            <td>
                <a href="@Url.Action("OrderMaterials","Singapore", new { id = item.MaterialID })">Order More</a>
            </td>
        </tr>
    }

</table>

<p>
    @Html.ActionLink("Add New Materials", "Create")
</p>

<br />
<br />
<a href="@Url.Action("Index","Singapore")">Return to Home page</a>


